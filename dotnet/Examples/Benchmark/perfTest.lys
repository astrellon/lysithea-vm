; A simple performance test script.
(function main ()
    (define total 0)
    (define counter 0)

    (loop (< counter 1000000)
        (set total (+ total (step)))
        (++ counter)
    )

    (print "Done: " total)
)

(function step ()
    (return (+ (rand) (rand)))
)

(main)