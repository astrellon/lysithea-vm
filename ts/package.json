{
  "name": "simple-stack-vm",
  "version": "1.0.0",
  "description": "A simple stack based virtual machine.",
  "scripts": {
    "perfTest": "ts-node ./src/perfTestIndex.ts",
    "diagTree": "ts-node ./src/dialogueTreeIndex.ts",
    "runCommands": "ts-node ./src/runCommandsIndex.ts",
    "tsc": "tsc && terser --compress --mangle --output simple-stack-vm.mini.js -- simple-stack-vm.js",
    "init": "tsc --init"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tsconfig/node18": "^1.0.1",
    "terser": "^5.15.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.2"
  }
}
